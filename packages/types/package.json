{
  "name": "@whitepine/types",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src/**/*.ts"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf index.d.ts src/**/*.d.ts",
    "lint": "echo 'No linting configured for types package'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests configured for types package'",
    "watch": "tsc --build --watch"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "mongoose": "^8.18.1"
  }
}
