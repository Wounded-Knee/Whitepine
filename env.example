# MongoDB Atlas Connection String
# Replace with your actual MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://x:y@whitepine.d4fml9k.mongodb.net/?retryWrites=true&w=majority&appName=Whitepine

# Optional: MongoDB Atlas specific settings
MONGODB_OPTIONS={
  "useNewUrlParser": true,
  "useUnifiedTopology": true,
  "maxPoolSize": 10,
  "serverSelectionTimeoutMS": 5000,
  "socketTimeoutMS": 45000
}

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret (for authentication - add when implementing auth)
JWT_SECRET=your_jwt_secret_here

# Mapbox Access Token (for Deck.gl map component)
# Get your free token at https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_TOKEN=abcde

GOOGLE_CLIENT_ID=abcde
GOOGLE_CLIENT_SECRET=abcde

OPENAI_API_KEY=abcde
